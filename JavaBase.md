# Java语言基础课程
##1.计算机系统机制概述
- 数制是一组固定的符号和统一的额规则来表示数值的方法
- 计算机底层使用的数制是二进制
- 用Java编程使用的是十进制。Java底层仍使用二进制
- 计算机常用的数制还有八进制和十六进制

### 1.1十进制
>十进制的基本数字是0~9 ，逢十进位。
>10称作“基数”，10^n（10的n次幂）被称作“权”

----
### 1.2 二进制
>二进制的基本数字是0、1，逢2进位。
>二进制的基数为2，权为2^n（2的n次方）
>（1111）二进制转为十进制    --->（15）

### 1.3 十六进制
>十六进制是二进制的简写，方便专业人员书写二进制数据；十六进制的基数为16，权为16^n （16的n次方）；
>再Java代码中十六进制数用0x或0x做前缀（0是数字零，而不是字母O）
>0x1000=1*16^3     0x1000=1*16^3


### 1.4 十六进制与其他进制的转换
>41的十六进制   等价于  65 的十进制
>16进制的1位数代表2进制的4位数。
>41的十六进制等价于    0100 0001 的二进制

### 1.5 二进制转换为十进制/十六进制
```
二进制转为十进制
   (10110)2 = (22)10

二进制转为十六进制
	(11010110)2= (D6)16
```
>规律：二进制转为十六进制数相当于一位十六进制数。

>计算机最基本的存储单位是字节（byte），一个字节有8位。8位2进制的最大值=（11111111）2	

### 1.6 十进制数转化为二进制数/十六进制数
>转二进制数规律：不断除以2，保留余数，商为0时，不在除2，将所有余数倒序排序
>（13）10  = （1101）2

----
>转十六进制数规律：不断除以16，保留余数，商为0时不再除16，将所有余数倒序排序。
>（138）10 = （8A）16


****
## 2.变量与数据类型
###2.1 数据类型初阶
```
Java定义了基本数据类型、引用数据类型和自定义数据类型。
	包括八种基本数据类型和引用数据类型中的String类型
```
- 基本数据类型如下
![](https://img2018.cnblogs.com/blog/1231979/201903/1231979-20190318172722491-17689725.png)


### 2.2 栈空间
>栈空间存取数据的效率高
>栈中的数据按“先进后出”的方式管理
>栈空间存储空间较小，不能存放大量的数据
>JVM将基本类型的数据存放在栈空间

![](https://img2018.cnblogs.com/blog/1231979/201903/1231979-20190318173546678-2146088565.png)

### 2.3 堆空间
>堆空间存取数据的效率最低；
>数据存放的位置随机分配
>堆空间存储数据的空间大，能存放大容量的数据。例如：数组、对象、字符串、集合、引用数据类型

![](https://img2018.cnblogs.com/blog/1231979/201903/1231979-20190318173729685-356386477.png)


### 2.4 变量的定义和变量使用的原因
- 什么是变量
```
·变量就是可变的量
·常量就是不可变的量
·字面量：Java的变量和常量中存放的具体的数据称为字面量
```

- 为什么使用变量

![](https://img2018.cnblogs.com/blog/1231979/201903/1231979-20190318174802704-1215827478.png)

![](https://img2018.cnblogs.com/blog/1231979/201903/1231979-20190318175106813-387894350.png)

>使用变量来管理内存中存储的数据；变量管理引用类型的数据


### 2.5 变量的命名和定义
```
(1)首字母是英文字母、$或下划线，由字母、数字、下划线组成；
(2)量的命名遵循见名知义的原则。
(3)Java变量名建议不用中文
(4)变量名首字母建议不用大写字母。
(5)用驼峰命名法命名多个单词组成的变量名。
(6)变量名不要使用Java关键字
```

- 定义变量以及初始化
```
每个变量必须属于一种数据类型。
   定义格式：类型 变量名1,变量名2,...变量n

    例如：byte score;
         short age;
         int sumScore;
         long fileSize;
         float x,y;
         double resule;
         char sex;
         boolean isContinue;
         String name;

name = "legend"
```

- 用变量保存多种数据类型的数据
```
package com.legend.HAND;

public class test3 {
    
    public static void main(String[] args){
        String name = "张飞";
        char sex = '男';
        int age = 35;
        float height = 1.99f;
        String type = "豪放的";
        System.out.println("Hello,I am"+name+"the age is"+age+"I am a"+type+sex);

        name = "王菲";
        sex = '女';
        age = 13;
        height = 1.50f;
        type = "奔放";
        System.out.println("Hello,I am"+name+"the age is"+age+"I am a"+type+sex);
    }

}
```

### 2.6 变量的所用域
>在一对花括号之间存在，脱离了便失效


### 2.7 基本数据类型的包装类
>Java为基本数据类型提供了对应的类，这些类称作为包装类。
![](https://img2018.cnblogs.com/blog/1231979/201903/1231979-20190318194401123-512089591.png)
 
 - 为什么使用包装类
>包装类中封装了一些很实用的方法和变量。例如：Byte.MIN_VALUE是Byte类中的一个常量，存放了byte类型数据的最小值。
>包装类在集合中用来定义集合元素的类型

- 包装类中常用的方法和变量
```
(1)Integet.MIN_VALUE：int类型的最小值：-2^31
(2)Integet.MAX_VALUE：int类型的最大值：2^31-1
(3)int Integer.parseInt(String sInteger);
   作用：将字符串类型的整数转换为int类型的数据

(4)String Integer.toBinaryString(int value);
    作用：将十进制数转换为二进制，返回结果String类型

(5)String Integer.toHexString(int value);
	作用：将十进制数转化为十六进制数，返回结果为String类型。

其他数据类型也是差不多的
```

### 2.8 二进制补码
```
计算机系统的内部以二进制形式存储数据。
在Java程序中输入的十进制的数据都会被自动转换为二进制，Java内部也以二进制数来进行数值运算，但返回的结果是十进制。
```
- 补码规则
```
在计算机系统中，数值一律用二进制的补码来存储。
二进制的最高为是符合位，0表示正数，1表示负数

正数的值是其本身，负数的值是最高位不变，其他逐位取反，再加1
```
>（1101）2     ==》取反 0010   再加1   0011    符号位不变   -3

>两数相加，若最高位（符号位）有进位，则进位被舍弃。


- 为什么使用补码
```
1.可以将符号位和其他位统一处理；
2.最高为不再表示数值，二十作为符号位，正好将数值折半，即一般是0至正数，一半是负数。
3.减法也可以按加法来处理；
```

- 补码运算的特征
```
计算机中正数和负数的关系是取反加一
```

- 补码运算的原理
```
正数+负数=模
  
  模：某种类型数据的总数
```

### 2.9 整数类型
```
Java有四种整数类型：byte、short、int和long
Java默认整数计算的结果是int 类型。
整数的字面量是int类型
若字面量超过int类型的最大值，则字面量是long类型，那么后面要用L表示该值是long类型。
```

### 3.0 浮点类型
```
浮点类型用于表示小数的数据类型。
浮点数原理：也就是二进制数科学记数法。
Java的浮点类型有float金额double两种。
Java默认浮点类型计算的结果是double类型，字面量也是double类型。
    若要给float类型的变量赋字面量的值，则要在字面量后面加F(f)
```
>浮点运算就是实数运算，由于计算机只能存储整数，所以实数都是约数，这样浮点运算是很慢的而且还会有误差。
![](https://img2018.cnblogs.com/blog/1231979/201903/1231979-20190318213030685-781796213.png)

### 3.1 字符类型
```
char类型的字面量可以是一个英文字母、字符或一个汉字，并且由单引号包括。

Java底层使用一个16位的整数来处理字符类型，该数值是一个字符的unicode编码值。


Java在处理char类型的数据时，在底层是按unicode码来处理的。
```

### 3.2 转义符
```
转移字符是'\n'  ，通过转义字符，克表示一些特殊的字符。
'\u4e2d'  表示Unicode编码对应的字符（汉字：中）
\\  表示 \
'\''  表示单引号
'\''' 表示双引号
\t  表示制表位字符
```